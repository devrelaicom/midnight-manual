manifest_version: 1
root:
  name: midnight-awesome-dapps
  path: .
  # Curated-list repo: markdown prose only (README list, style guide,
  # dormant_projects/). CI/renovate config is not signal.
  include: ["**/*.md", "**/*.mdx"]
  provenance:
    attribution: foundation
    verified: false
    verified_by: midnight-foundation
    tags: [trust:high]
