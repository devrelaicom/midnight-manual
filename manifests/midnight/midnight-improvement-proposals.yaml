manifest_version: 1
root:
  name: midnight-improvement-proposals
  path: .
  # Pure-markdown proposals repo (mips/, mps/, deployments/) with no site app
  # chrome — admit the prose only; CI/renovate config is not signal.
  include: ["**/*.md", "**/*.mdx"]
  provenance:
    attribution: foundation
    verified: true
    verified_by: midnight-foundation
    tags: [trust:high]
