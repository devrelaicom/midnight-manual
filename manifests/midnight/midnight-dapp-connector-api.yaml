manifest_version: 1
root:
  name: midnight-dapp-connector-api
  path: .
  exclude:
    # Generated TypeDoc API reference (typedoc.json out=docs/api) — the
    # canonical copy lives in midnight-docs/api-reference/. Handwritten
    # QUICKSTART/SPECIFICATION/RELEASE_NOTES and src/ are kept.
    - "docs/api/**"
  provenance:
    attribution: foundation
    verified: true
    verified_by: midnight-foundation
    tags: [trust:high]
