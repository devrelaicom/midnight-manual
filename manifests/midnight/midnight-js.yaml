manifest_version: 1
root:
  name: midnight-js
  path: .
  exclude:
    # Generated TypeDoc API reference — the canonical copy lives in
    # midnight-docs/api-reference/. Handwritten docs/releases/ is kept.
    - "docs/api/**"
  provenance:
    attribution: foundation
    verified: true
    verified_by: midnight-foundation
    tags: [trust:high]
