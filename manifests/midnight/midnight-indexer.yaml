manifest_version: 1
root:
  name: midnight-indexer
  path: .
  exclude:
    # Static test data / fixture dumps (blocks.json, transactions.json, …).
    - "qa/tests/data/**"
    # Test-run evidence artifacts (md/xml golden output).
    - "qa/tests/evidence/**"
  provenance:
    attribution: foundation
    verified: true
    verified_by: midnight-foundation
    tags: [trust:high]
