manifest_version: 1
root:
  name: midnight-node-docker
  path: .
  exclude:
    # Cardano genesis blobs (protocol-parameter data dumps, not setup docs).
    # Readable stack config (config.json/topology.json/pc-chain-config.json),
    # compose files, and scripts are kept.
    - "**/*-genesis.json"
  provenance:
    attribution: foundation
    verified: false
    verified_by: midnight-foundation
    tags: [trust:high]
